ICS for the T-Mobile SGH-T849
===========

To initialize your local repository use a command like this:

    repo init -u git://github.com/sbradymobile/android.git -b ics

Then to sync up:

    repo sync
